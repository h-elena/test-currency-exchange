{
    "name": "h-elena/test-currency-exchange",
    "description": "The task on currency exchange",
    "keywords": [],
    "type": "project",
    "autoload": {
        "psr-4": {
            "v1\\": "v1"
        }
    },
    "require": {
        "php": ">=7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^8"
    },
    "config": {
        "sort-packages": true,
        "bin-dir": "bin"
    },
    "scripts": {
        "phpunit": "phpunit"
    }
}
